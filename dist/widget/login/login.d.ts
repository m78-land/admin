/// <reference types="react" />
import { TaskLoginProps } from '../../types/types';
/**
 * 内置的登录样式，非必选
 * */
declare const Login: ({ logo, title, desc, content }: TaskLoginProps) => JSX.Element;
export default Login;
