import { WindowLayoutSectionProps } from '../../types';
/**
 * 提供可选的窗口基础的内容块，作为WindowLayout的子项时，会自动在其侧栏生成tab
 * */
export default function WindowLayoutSection(props: WindowLayoutSectionProps): JSX.Element;
